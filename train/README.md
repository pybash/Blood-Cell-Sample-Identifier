# Model Training Tools

This is a small Python Script that will take data annotations and train them onto a YOLOv11 model.

Seperate the images and the values into different folders (i.e images in "current_dataset/images/train" and the values in "current_dataset/images/train").

Once the script starts, it will clone those folders into another folder in "datasets/" with a dated directory before training the model. 